language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"

matrix:
  include:
  - python: 3.7
    dist: xenial
    sudo: true

install:
  - pip install -r requirements-test.txt

script:
  - pytest -v tests/ --cov=m2cgen/
  - flake8 .

after_success:
  - coveralls

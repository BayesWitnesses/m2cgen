language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"

install:
  - pip install -r requirements-test.txt

script:
  - pytest -v tests/ --cov=m2cgen/ --durations=0
  - flake8 .

after_success:
  - coveralls

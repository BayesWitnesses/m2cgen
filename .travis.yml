language: python

python:
  - "3.6"

before_install:
  - sudo apt-get install g++

install:
  - pip install -r requirements-test.txt

script:
  - pytest -v tests/ --cov=m2cgen/
  - flake8 .

after_success:
  - coveralls
